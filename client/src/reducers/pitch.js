function pitch (state = {}, action) {
  switch (action.type) {
    case 'SELECT_PITCH':
      return state;
    case 'ADD_PITCH':
      return state;
    case 'UPVOTE':
      return 
    default:
      return state;
  }
}

function pitchList (state={}, action) {
  switch (action.type) {
    case 'ADD_PITCH':
    default:
      return state;
  }
}